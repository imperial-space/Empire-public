uniform sampler2D SCREEN_TEXTURE;


void fragment() {
    highp vec2 screenUV = FRAGCOORD.xy * SCREEN_PIXEL_SIZE;

    highp vec2 amt = 1.0 / 10.0 / SCREEN_PIXEL_SIZE;
	highp vec2 uv = round(screenUV * amt) / amt;
	highp vec4 c = zTextureSpec(SCREEN_TEXTURE, uv);

	COLOR = c;
}
